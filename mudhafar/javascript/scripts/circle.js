var circleRadius = 20;
var circomference = Math.round( 2 * circleRadius * Math.PI );
var area = Math.round(Math.pow(circleRadius,2) * Math.PI);
console.log("For circle radius " + circleRadius + "cm, the cicomference is " + circomference + " and the area is " + area);