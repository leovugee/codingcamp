var myAge = 36;
var maxAge = 80;
var numSnackDay = 2;
var numSnack = numSnackDay * 360 * (maxAge - myAge);
var message = " You will need " + numSnack + " to last you until the ripe old age of " + maxAge;
console.log(message);