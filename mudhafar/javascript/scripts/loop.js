//var mul;
function mulfun() {
	//mul = document.getElementById('multiplicator').value;
	for (var mul = 0; mul <= 10; mul++){
	for (var counter = 0; counter <= 10; counter++) {
		console.log(counter + " * " + mul + " = " + counter * mul);
	}
}
}
mulfun();