var radius = 3 ;
var circumference = Math.round(2 * radius * Math.PI );
var area= Math.round(Math.pow(radius,2) * Math.PI ) ;
document.write("this is the circumference " + circumference + " this the area " + area);
