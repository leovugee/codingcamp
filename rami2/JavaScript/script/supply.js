var curAge = 30;
var maxAge = 50;
var eegs = 3;
var remainingAge = maxAge - curAge;
var allEegs = (eegs * 365) * remainingAge;
console.log(" you will need " + allEegs + " to last you until the ripe old age of " + maxAge);