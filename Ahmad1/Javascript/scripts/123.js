var  multiplier =10;
var total = 10;

for(multiplier = 1; multiplier <= total; multiplier++) {

	for(var i = 0; i <= total; i++) {
		var result  = multiplier * i;
		console.log(+ i + " * " + multiplier + " = " + result);
	}

}
