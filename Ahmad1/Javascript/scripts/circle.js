var radius = 4;
var circumference = Math.round(2 * radius * Math.PI) ;
var area = Math.round(Math.pow(radius, 2) * Math.PI) ;
document.write("this is the circumference " + circumference + " and this is the area " + area);
